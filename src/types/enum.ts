export enum SourceType {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}

export enum ContactSortBy {
  ID = 'id',
  NAME = 'name',
}

export enum TagSortBy {
  ID = 'id',
  NAME = 'name',
}

export enum FileType {
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
}
